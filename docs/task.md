# fitTODOay — Активные задачи

## Следующий этап (LLM + UX)
- [x] Переработать prompt для post-workout LLM: ответы короче, конкретнее, без «воды», с чёткими шагами действия.
- [ ] Рассмотреть удаление отдельной кнопки «Рекомендации», если LLM-поток полностью покрывает рекомендации и применение изменений.
- [x] Зафиксировать тактику IRR в LLM-логике: применять только к упражнениям, где метрика уместна (силовые/повторные), и не форсировать для timed/специфичных движений.
- [ ] Расширить анализ LLM не только текущим днём: учитывать историю других тренировочных дней и недельные тренды по программе.
- [x] В чате вместо сводки «N изменений» показывать конкретный список предложенных правок в человекочитаемом виде (упражнение, что меняется, новое значение).
- [x] Исправить вибрацию на мобильном Chrome: проверить ограничения Web Vibration API (user gesture/таймер/visibility), добавить fallback/диагностику и обеспечить срабатывание в конце отдыха.

## Следующий этап (Feedback API + triage)
- [ ] Добавить `GET /api/feedback/` для чтения отзывов текущего пользователя (сохранить `POST /api/feedback/` без ломающих изменений).
- [ ] Добавить пагинацию и фильтры для `GET /api/feedback/`: `limit`, `offset`, `from`, `to`, `q`.
- [ ] Добавить staff-endpoint `GET/PATCH /api/admin/feedback/` для triage отзывов.
- [ ] Расширить модель `UserFeedback` полями triage: `status`, `category`, `priority`, `task_ref`, `admin_note`, `reviewed_at`, `reviewed_by`.
- [ ] Добавить в админку Django удобные фильтры/поиск по triage-полям.
- [ ] Добавить тесты прав доступа: пользователь видит только свои отзывы, не-staff не имеет доступа к admin feedback.
- [ ] Добавить тесты фильтров и обновления triage-статусов через admin endpoint.
- [ ] Обновить `docs/test-plan.md` и краткую API-документацию по feedback endpoints.

## Следующий этап (Мобильные уведомления / отдых)
- [ ] Нет вибрации при выключенном экране телефона: исследовать ограничения ОС/браузера (Chrome Android, background/lock state), добавить fallback (звук/notification) и документировать ожидаемое поведение.

## Следующий этап (UX инфо упражнения)
- [ ] В окне «Инфо упражнения» вынести график прогресса в отдельную вкладку «Статистика», а в основной вкладке оставить главный акцент на фото и описании упражнения.

## Backlog (низкий приоритет)
- Ограничения бесплатного тарифа по числу пользовательских упражнений/шаблонов.
- Персонализация дефолтных параметров упражнений на основе профиля.
- Экспорт/импорт программ (CSV/JSON) — рассмотреть после MVP.
- Интеграция носимых устройств для дополнительных метрик (калории/HR).
- Рекомендации по прогрессии анализируют только последний завершённый день (нужен отдельный бэклог на историю, если захотим смотреть тренды).
- Добавить накопительные задачи по смягчению автофиксации веса (например, подтверждение несколькими днями), чтобы избежать реакций на одиночные отклонения.
- LLM‑рекомендации на основе истории: отправлять упражнения/логи за последний месяц и получать новую программу с корректировками.
