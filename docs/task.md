# fitTODOay — Активные задачи

## 1. Подготовка LLM-агента
- [x] Добавить переменные в конфигурацию (`OPENROUTER_API_KEY`, `OPENROUTER_MODEL`), описать их в README/пример `.env`.
- [x] Спроектировать хранение пользовательских предпочтений ассистента (например, поле `llm_preferences` в профиле) и API для чтения/записи.
- [x] Реализовать backend endpoint `POST /api/llm-agent/programs/`:
  - принимает параметры визарда,
  - обращается к OpenRouter, валидирует ответ,
  - создаёт ProgramFolder/DayTemplate/TemplateExercise (одна активна, остальные выключены),
  - логирует запрос/ответ, обрабатывает ошибки.
- [x] Добавить fallback-сообщение «создание через помощника недоступно…» для ошибок LLM.
- [x] Перейти на новый каталог упражнений (`Exercise_DB` + `ExerciseMuscle`/`ExerciseInstruction`/`ExerciseImage`) во всех сервисах:
  - обеспечить миграцию данных из текущей таблицы `exercises_exercise` и синхронизировать идентификаторы;
  - обновить сериализаторы и сервисы LLM, чтобы брать список упражнений из новой схемы;
  - адаптировать `TemplateExercise`/прочие модели, которые сейчас ссылаются на старый справочник, и добавить миграции;
  - пересобрать скрипты импорта (`import_exercise_db.py`) и тестовые фикстуры, чтобы они наполняли новую структуру;
  - покрыть изменения тестами и обновить документацию (PRD, API) перед выкатыванием.

## 2. Визард на фронтенде
- [x] Добавить раздел «Создать с помощником»:
  - последовательные шаги (пол, возраст, вес, цель, тренировки/неделя, длительность),
  - автозаполнение из сохранённых предпочтений, возможность редактировать.
- [x] Экран результата: показать созданные программы, позволить активировать/пересоздать или перейти к ручному редактированию.
- [x] Обработка ошибок/статусов (индикатор генерации, сообщение при недоступности агента).

## 3. Инфраструктура и логирование
- [x] Централизованное логирование обращений к LLM (ID пользователя, время, обезличенные payload/ответ, код ошибки).


## Backlog (низкий приоритет)
- Перезаполнить описание упражнений (подробное описание техники выполнения).
- Ограничения бесплатного тарифа по числу пользовательских упражнений/шаблонов.
- Персонализация дефолтных параметров упражнений на основе профиля.
- Экспорт/импорт программ (CSV/JSON) — рассмотреть после MVP.
- Интеграция носимых устройств для дополнительных метрик (калории/HR).
- Состояние выполненных упражнений и фактических значений должно сохраняться офлайн (при недоступном интернете) и синхронизироваться после восстановления сети.
