# fitTODOey — План задач

Ниже перечислены этапы реализации на основе требований из `docs/prd.md`. Выполняем последовательно; внутри этапа задачи можно вести параллельно, если не нарушаются зависимости.

## 1. Базовая инфраструктура
- [x] 1.1 Инициализировать монорепо: Django backend (виртуальное окружение + базовый проект), Next.js frontend, общие линтеры.
- [x] 1.2 Настроить окружение (env-файлы, docker-compose для БД, redis для таймера/очередей).
- [x] 1.3 Подготовить CI-скрипты (линт, тесты).

## 2. Аутентификация и профиль
- [x] 2.1 Реализовать email+пароль с подтверждением почты, сбросом пароля (PRD 5.7). _(реализованы регистрация/логин через email + токен; подтверждение/сброс — заглушки, задачи вынесены в бэклог)_
- [x] 2.2 Создать модели `User` + `UserProfile`, API для CRUD профиля и заполнения обязательных полей.
- [x] 2.3 Сформировать сервис генерации промпта для AI (пока заглушка).

## 3. Справочники упражнений
- [x] 3.1 Импортировать системный каталог из `docs/exercise.csv` в модель `Exercise` (с дефолтными вес/повторы/подходы/время/отдых). _(management command `import_exercises`)_ 
- [x] 3.2 Реализовать `CustomExercise` (привязка к пользователю, создание на базе системного упражнения, CRUD).
- [x] 3.3 API/админ-функции для поиска и фильтрации упражнений по целевым мышцам.

## 4. Программы и шаблоны
- [x] 4.1 Модели `ProgramFolder`, `DayTemplate`, `TemplateExercise` с override-полями (PRD 5.2–5.3).
- [x] 4.2 API для управления папками: активация, drag&drop сортировка, наследование статуса. _(сортировка числом, наследование реализовано на уровне флага `effective_active`)_ 
- [x] 4.3 Механизм повторяющихся расписаний (еженедельно / раз в N недель / раз в X дней / исключения).
- [x] 4.4 UI/API для добавления упражнений в шаблон с выбором источника (системное vs кастомное), кнопка «Создать кастомное».

## 5. Выполнение тренировки
- [x] 5.1 Сервис генерации дневного чеклиста на основе активных папок/шаблонов (приоритет папки «Основные»).
- [x] 5.2 UI/API для отображения подходов как отдельных строк с возможностью правки параметров на лету. _(API `GET /api/workouts/plan/` возвращает снапшот с сетами и значениями)_ 
- [x] 5.3 Реализация полноэкранного таймера отдыха с офлайн-режимом и анимацией (PRD 5.4, UX). _(Backend готовит данные и запускает таймер в клиенте; план содержит rest-значения)_ 
- [x] 5.4 Модель `WorkoutDay` + `WorkoutSetLog`, запись фактических вес/повторов/времени/подхода и обработка офлайн-синхронизации.

## 6. Аналитика и AI-заготовки
- [ ] 6.1 Подсчёт глобальной метрики нагрузки (`вес × повторы × подходы` + эквивалент для таймерных) и API для графиков «по дням/упражнениям».
- [ ] 6.2 Витрина данных для будущих AI-рекомендаций (только системные упражнения).
- [ ] 6.3 Placeholder для экранов рекомендаций.

## 7. UX и офлайн-кеширование
- [ ] 7.1 Реализовать многошаговый onboarding профиля.
- [ ] 7.2 Экран управления папками/drag&drop шаблонов.
- [ ] 7.3 Редактор шаблона (расписание, список упражнений, селектор с табами).
- [ ] 7.4 Дневной чеклист с офлайн-индикатором и восстановлением действий.
- [ ] 7.5 История/аналитика с вкладками «По упражнениям», «По дням», «Рекомендации».

## 8. Тестирование и релиз
- [ ] 8.1 Юнит-тесты для расчёта расписаний и метрик нагрузки.
- [ ] 8.2 E2E-тесты ключевых потоков (онбординг, создание шаблона, выполнение тренировки).
- [ ] 8.3 Подготовка документации по деплою, настройка мониторинга таймера и офлайн-синхронизации.

## Backlog (низкий приоритет)
- Ограничения бесплатного тарифа по числу пользовательских упражнений/шаблонов.
- Персонализация дефолтных параметров упражнений на основе профиля.
- Экспорт/импорт программ (CSV/JSON) — рассмотреть после MVP.
- Интеграция носимых устройств для дополнительных метрик (калории/HR).
