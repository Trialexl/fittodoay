# URL для API, доступного из браузера (локально через HTTP)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Stable key for Next.js Server Actions across rebuilds/deploys
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=change-me-to-a-stable-32-byte-base64-key
