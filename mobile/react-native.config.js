/**
 * React Native CLI configuration.
 * Explicitly sets Android package name so autolinking can read it.
 */
module.exports = {
  project: {
    android: {
      packageName: "com.fittodoay.app",
    },
  },
};
