# Перенос фронтенда в мобильное приложение

## 1. Базовая архитектура
- [x] 1.1 Настроить React Native стек из PRD: навигация (AuthStack + MainTabs), хранилище сессии/предпочтений через AsyncStorage, сетевой слой `apiFetch` с токеном и обработкой 401 (аналог `@fittodoay/shared`).
- [x] 1.2 Вынести глобальные провайдеры: Auth/Theme/Query (или SWR-эквивалент), OfflineBanner, обработка invalid token (аналог `notifyInvalidToken`).
- [x] 1.3 Согласовать `.env`/конфиги (`API_URL`, модель ассистента) и базовую типизацию запросов.

## 2. Экран приветствия/авторизации
- [x] 2.1 Реплицировать логику HomePage: быстрый вход по email/паролю, при ошибке 400/404 — авто-регистрация с дефолтным профилем (`buildDefaultProfile`).
- [x] 2.2 Экран `login`/`register`: редирект по токену, хранение статуса загрузки/ошибок, переход к `workout`.

## 3. Онбординг
- [x] 3.1 Многошаговый мастер (из `OnboardingWizard`): email/пароль/имя + профиль (goal/level/gender/age/weight/height/equipment/limitations/schedule notes).
- [x] 3.2 Локальное состояние между шагами, отправка `POST /api/auth/register/`, после успеха — на `programs`.
- [x] 3.3 UI: прогресс по шагам, валидация обязательных полей, кнопки Назад/Далее.

## 4. Темы и акцент
- [x] 4.1 Порт `ThemeContext`: хранение темы/акцентного цвета, загрузка/сохранение в AsyncStorage и `/api/profile/preferences/`, применение CSS-переменных аналогом в RN (StyleSheet/theme provider). (синхронизация с API позже)
- [x] 4.2 UI для выбора темы (light/dark) и палитры + кастомный цвет.

## 5. Шапка/меню и офлайн
- [x] 5.1 AppHeader эквивалент: навигационные ссылки (`Программы/Чеклист/Аналитика/Помощник`), статус пользователя, выход.
- [x] 5.2 Фидбек модалка: POST `/api/feedback/`, состояния отправки/ошибок/успеха. (кнопка без модалки)
- [x] 5.3 OfflineBanner: детекция offline, фиксированный баннер.

## 6. Программы (ProgramBoard)
- [x] 6.1 Список папок (`/api/programs/folders/`): expand/collapse, теги активности, кнопки «Новая программа», «Создать с помощником».
- [x] 6.2 CRUD программы в модалке: имя/комментарий/флаг активности, DELETE (кроме защищённых «Основная/Основные»).
- [x] 6.3 Шаблоны папки (`/api/programs/templates/?folder=...`): карточка с раскрытием, мышцы (агрегация), кнопки «Добавить упражнение», «Настроить шаблон».
- [x] 6.4 DnD сортировка упражнений в шаблоне (`template-exercises/reorder`): на мобильном — жесты/SortableList с оптимистичным UI и блокировкой при `reorderingTemplateId`.
- [x] 6.5 Модалка шаблона (`TemplateEditor`): выбор папки, имя/комментарий, расписания weekly/biweekly/interval/custom с валидаторами, сохранение/удаление.
- [x] 6.6 Модалка упражнения: поиск `/api/exercises/?q=`, предзаполнение default reps/sets/weight/time/rest, флаги `has_weight/has_time`, активность, note, sort_order, DELETE. Предпросмотр описания/картинок/мышц/сложности.
- [x] 6.7 LLM-чат по папке: создание треда `/api/llm-agent/threads/`, сообщения `/messages/`, отображение `actions` из последнего ответа, кнопка «Применить изменения» (`/apply/`), обработка ошибок/лоадеров.

## 7. Помощник (LLM wizard)
- [x] 7.1 Выровнять мастер с вебом: поля `gender/age/weight_kg/height_cm/goal/sessions_per_week/session_duration/notes` + опыт/оборудование/ограничения, stepper и текущее состояние шага, валидация обязательных полей, кнопки Назад/Далее/Отправить.
- [x] 7.2 Префил и сохранение `llm_preferences`: подтягивать из `/api/profile/preferences/` при открытии, сохранять изменённые ответы обратно (как на вебе) и повторно использовать при новом запуске.
- [x] 7.3 POST `/api/llm-agent/programs/`: корректный payload, лоадер/disable контролов, отображение созданных программ (активная/неактивные, summary, дни), CTA «к программам/к чеклисту/сгенерировать заново».
- [x] 7.4 Fallback UX: дружелюбное сообщение на 503/невалидный JSON (см. AGENTS.md), показ сырого текста ошибки в debug, кнопка «Попробовать снова».

## 8. Чеклист тренировки
- [x] 8.1 Экран `workout`: загрузка плана `/api/workouts/plan/?date=...`, сохранение выбранной даты, today muscles/нагрузка, выбор даты через календарь (month grid, диапазон загрузок `/api/analytics/days/`).
- [x] 8.2 Структура: папка → шаблон → упражнения → подходы; сворачивание по умолчанию (незавершённые открыты).
  - [x] 8.2.1 Сохранять состояние сворачивания между заходами (AsyncStorage) и по умолчанию открывать только незавершённые элементы.
  - [x] 8.2.2 Инкрементальные контролы (+/− с удержанием) для плановых/фактических значений сетов.
- [x] 8.3 Выполнение сетов: для time-упражнений Execution overlay (таймер, callouts, прерывание/выполнить досрочно). После выполнения — Rest overlay (таймер, collapse бар, поля reps/weight/time, skip/restart).
  - [x] 8.3.1 Связать Execution → Rest с сохранением фактических значений.
  - [x] 8.3.2 Очистка стейтов и блокировка скролла при активных оверлеях.
  - [x] 8.3.3 Collapse/progress бар и кнопка restart в Rest overlay (паритет с веб).
- [x] 8.4 Отправка логов `/api/workouts/logs/`, инкрементальные контролы (+/− с удержанием, шаги 1/2/1сек), форматирование значений.
  - [x] 8.4.1 Форматирование и отправка фактических значений, пересчёт прогресса.
  - [x] 8.4.2 Рефреш плана/очереди после успешной отправки.
- [x] 8.5 Офлайн-очередь (аналог `useOfflineWorkoutQueue`): AsyncStorage хэш по `workout_day/template_exercise/set_index`, авто-синхронизация при online, баннер состояния/ошибки.
  - [x] 8.5.1 Баннер состояния/счётчик очереди + индикация ошибок синка, декремент при синхронизации.
- [x] 8.6 Редактирование/удаление логов в модалке (`PATCH/DELETE /api/workouts/logs/{id}/`), предзаполнение значений/валидация.
  - [x] 8.6.1 Модалка редактирования с валидацией, ошибки/успех.
  - [x] 8.6.2 Удаление лога, пересчёт прогресса.
- [x] 8.7 Инфо по упражнению: описание, мышцы, сложность, изображения (carousel), note, ссылка/CTA на ProgramBoard.
- [x] 8.8 Рекомендации после завершения папки: загрузка `/api/workouts/recommendations/?date=...`, формы по упражнениям (reps/weight), apply `/apply/` c сообщениями об ошибках/успехе и фокусом на первом поле.

## 9. Аналитика
- [x] 9.1 Карточки нагрузки по дням (`/api/analytics/days/`): кликабельные точки с переходом на workout с датой, skeletons/ошибки/пустые состояния, синхронный рефреш с чеклистом.
- [x] 9.2 Топ упражнений (`/api/analytics/exercises/`): сортировки/фильтры как на вебе, переход к ProgramBoard/чеклисту для упражнения, state empty/loading.
- [x] 9.3 Program Trends: диапазоны (week/month/half-year/year), view `programs/exercises`, granularities `day/week`, типы графиков (line/area/stacked/columns/heatmap/pie/radar/scatter/area/line), выделение серии, расчёт интерполяции пустых точек, горизонтальная прокрутка и tooltips адаптированные под мобильный.
  - [x] 9.3.1 Добавить переключатели типа графика (line/area/stacked/columns/heatmap/pie/radar/scatter) и view programs/exercises с выбором серии (folder/exercise) как на вебе.
  - [x] 9.3.2 Поддержать интерполяцию/заполнение пустых точек, подсветку активной серии, tooltips и горизонтальную прокрутку адаптированную под мобильный.
  - [x] 9.3.3 Подтянуть данные по структуре `folders/exercises/series` (как в веб ProgramTrendPanel) и отрисовать соответствующие графики.
- [x] 9.4 Placeholder AI-feed блок (как в вебе).

## 10. Профиль/настройки
- [x] 10.1 Экран профиля: просмотр/редактирование профиля, LLM preferences (синхронно с вебом и ассистентом), смена темы/акцента с отправкой на `/api/profile/preferences/`, logout.

## 11. Общие UI-компоненты
- [x] 11.1 Кнопки/инпуты/модалки с состояниями loading/disabled, доступность (Accessibility labels, focus traps), единый стиль как на вебе.
- [x] 11.2 BrandMark и анимации callout (Rest/Execution overlays) — убедиться в единообразии размеров/таймингов с вебом.
- [x] 11.3 Блоки пустых состояний (нет программ/нет активных шаблонов/нет тренировок) — копирайт/CTA как на вебе.

## 12. Сеть и ошибки
- [x] 12.1 Унифицированный обработчик ApiError (статус/сообщение/invalid token → logout), toasts/alerts, фолбэк при `OPENROUTER`/LLM ошибках.
- [x] 12.2 Индикация загрузок (скелетоны/спиннеры) для списков программ, чатов ассистента, чеклиста и аналитики.

## 13. Тесты и качество
- [ ] 13.1 Порт unit-тестов (React Native Testing Library + Jest) для: Auth flow, Onboarding wizard, ProgramBoard interactions (создание/редактирование/реордер), Assistant wizard, Checklist (логи, офлайн-очередь, таймеры), Analytics переключатели.
- [ ] 13.2 Настроить ESLint/Prettier/TypeScript под RN; CI команды `npm run lint`, `npm run test`, release сборки (ios/android) согласно PRD-mobile.
- [ ] 13.3 Проверить сценарии из `docs/test-plan.md` (LLM, повторный запуск с сохранёнными данными, обработка ошибок).

## 14. Проверки готовности
- [x] 14.1 Сверить функциональный паритет с `frontend` по всем маршрутам (`/`, `/login`, `/onboarding`, `/programs`, `/workout`, `/analytics`, `/assistant`).
- [x] 14.2 Убедиться в соответствии UX-гайдам PRD (таймеры офлайн, инпуты с удержанием, авто-редиректы по токену, дружелюбные fallbacks).
